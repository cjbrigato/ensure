//go:build freebsd
// +build freebsd

package freebsd

// This constant provides the definition needed by ensure_notfreebsd.go,
// but only when compiling for freebsd.
const error_compiling_this_package_requires_goos_freebsd = true
